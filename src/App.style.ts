import { StyleSheet } from "react-native";

/* App styles */

const styles = StyleSheet.create({
  main: {
    width: "100%",
    height: "100%",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
});

/* Exports */
export { styles };
