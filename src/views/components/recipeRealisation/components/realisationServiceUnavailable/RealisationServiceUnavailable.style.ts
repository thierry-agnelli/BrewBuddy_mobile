import { aspectRatio } from "@utils";
import { StyleSheet } from "react-native";

/**
 * RealisationServiceUnavailable styles.
 */

const styles = StyleSheet.create({
  realisationServiceUnavailable: {
    width: "100%",
    gap: aspectRatio("height", 10),
  },
});

/* Exports */
export { styles };
