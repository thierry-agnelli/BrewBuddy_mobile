import { StyleSheet } from "react-native";

/**
 * Fermentation
 */
const styles = StyleSheet.create({
  fermentation: {
    flex: 1,
    width: "100%",
    justifyContent: "space-around",
  },
});

/* Exports */
export { styles };
